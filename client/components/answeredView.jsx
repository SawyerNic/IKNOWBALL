const React = require('react');
const { useState, useEffect } = require('react');
const socket = io();

const AnsweredView = () => {

    return (
        <h1></h1>
    )
}

module.exports = {
    AnsweredView
};